add_library(mterm_core STATIC
  core/Policy.cpp core/FileService.cpp core/Canvas.cpp core/Store.cpp)
target_include_directories(mterm_core PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(mterm_core PUBLIC Qt6::Core Qt6::Sql)
if(MSVC)
  target_compile_options(mterm_core PRIVATE /W4 /permissive-)
else()
  target_compile_options(mterm_core PRIVATE -Wall -Wextra -Wpedantic)
endif()
if(BUILD_TESTING)
  add_executable(mterm-core-tests tests/CoreTests.cpp)
  target_link_libraries(mterm-core-tests PRIVATE mterm_core Qt6::Test)
  add_test(NAME native-core COMMAND mterm-core-tests -o core-tests.txt,txt)
endif()

add_library(mterm_runtime STATIC runtime/ProcessRunner.cpp runtime/ProcessInventory.cpp runtime/JsonLines.cpp)
target_include_directories(mterm_runtime PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(mterm_runtime PUBLIC mterm_core Qt6::Core)
if(WIN32)
 target_link_libraries(mterm_runtime PRIVATE psapi)
endif()
if(BUILD_TESTING)
 add_executable(mterm-test-child tests/TestChild.cpp)
 target_link_libraries(mterm-test-child PRIVATE Qt6::Core)
 add_executable(mterm-runtime-tests tests/RuntimeTests.cpp)
 target_link_libraries(mterm-runtime-tests PRIVATE mterm_runtime Qt6::Test)
 add_dependencies(mterm-runtime-tests mterm-test-child)
 add_test(NAME native-runtime COMMAND mterm-runtime-tests -o runtime-tests.txt,txt)
 set_tests_properties(native-core PROPERTIES COMMAND_EXPAND_LISTS TRUE)
endif()

add_library(mterm_services STATIC services/Backend.cpp services/WorkspaceSession.cpp services/JobService.cpp services/TerminalService.cpp)
target_link_libraries(mterm_services PUBLIC mterm_runtime mterm_terminal)
if(BUILD_TESTING)
 add_executable(mterm-backend-tests tests/BackendTests.cpp)
 target_link_libraries(mterm-backend-tests PRIVATE mterm_services Qt6::Test)
 add_dependencies(mterm-backend-tests mterm-test-child)
 add_test(NAME native-backend COMMAND mterm-backend-tests -o backend-tests.txt,txt)
endif()

if(MTERM_BUILD_DESKTOP)
 find_package(Qt6 6.5 REQUIRED COMPONENTS Gui Widgets)
 add_library(mterm_desktop STATIC desktop/MainWindow.cpp desktop/CanvasView.cpp desktop/JobPane.cpp desktop/TerminalWidget.cpp desktop/TerminalPane.cpp)
 target_link_libraries(mterm_desktop PUBLIC mterm_services Qt6::Widgets)
 add_executable(mterm WIN32 desktop/main.cpp)
 target_link_libraries(mterm PRIVATE mterm_desktop)
 install(TARGETS mterm RUNTIME DESTINATION bin)
 if(BUILD_TESTING)
  add_executable(mterm-desktop-tests tests/DesktopTests.cpp)
  target_link_libraries(mterm-desktop-tests PRIVATE mterm_desktop Qt6::Test)
  add_test(NAME native-desktop COMMAND mterm-desktop-tests -o desktop-tests.txt,txt)
  set_tests_properties(native-desktop PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
 endif()
endif()

# Unmodified MIT upstream parser; pin and source license travel with this repository.
add_library(mterm_vterm STATIC
 third_party/libvterm/src/encoding.c third_party/libvterm/src/keyboard.c
 third_party/libvterm/src/mouse.c third_party/libvterm/src/parser.c
 third_party/libvterm/src/pen.c third_party/libvterm/src/screen.c
 third_party/libvterm/src/state.c third_party/libvterm/src/unicode.c
 third_party/libvterm/src/vterm.c)
set_target_properties(mterm_vterm PROPERTIES C_STANDARD 99 AUTOMOC OFF)
target_include_directories(mterm_vterm PUBLIC third_party/libvterm/include PRIVATE third_party/libvterm/src)
add_library(mterm_terminal STATIC terminal/TerminalScreen.cpp terminal/PtySession.cpp)
target_link_libraries(mterm_terminal PUBLIC Qt6::Core mterm_vterm)
target_include_directories(mterm_terminal PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
if(WIN32)
 target_compile_definitions(mterm_terminal PRIVATE _WIN32_WINNT=0x0A00 WINVER=0x0A00 NTDDI_VERSION=0x0A000006)
endif()
if(BUILD_TESTING)
 add_executable(mterm-terminal-tests tests/TerminalTests.cpp)
 target_link_libraries(mterm-terminal-tests PRIVATE mterm_terminal Qt6::Test)
 add_test(NAME native-terminal COMMAND mterm-terminal-tests -o terminal-tests.txt,txt)
endif()
