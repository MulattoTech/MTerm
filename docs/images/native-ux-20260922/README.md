# Actual native UI screenshots

These screenshots were captured from the running Windows Qt application using the isolated
`responsiveWorkspaceAndOptionalVisualEvidence` test fixture. They are not mockups or Electron
screenshots. Cards are labeled fixture data, the workspace path is a generic display label, and
no model/DevFleet connection is active. No third-party artwork or font files are included.

Desktop 1500x940, compact 1100x780, Project view, and filtered command palette were visually reviewed.
Do not replace these with headless offscreen screenshots lacking usable fonts.
