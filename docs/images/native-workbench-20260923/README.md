# Actual native workbench screenshot

Captured from running Windows Qt using optionalWorkbenchVisualEvidence, with temporary workspace.md
and settings.json fixture files. Dirty tab and per-buffer text are real native UI state, not a mock.
Generic workspace display label; no model/DevFleet/network activity or third-party artwork/fonts.
